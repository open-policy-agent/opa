{
  "dependencies": {
    "@observablehq/notebook-kit": "^1.5.0"
  },
  "scripts": {
    "docs:preview": "notebooks preview",
    "docs:build": "notebooks build --no-minify --base /opa/benchmarks --empty --out ../benchmarks/ -- *.html"
  }
}
