---
cases:
  - note: globmatch/glob no match with character-range matchers
    query: data.generated.p = x
    modules:
      - |
        package generated

        p contains x if {
        	glob.match("[!a-c]at", [], "bat", x)
        }
    data: {}
    want_result:
      - x:
          - false
    sort_bindings: true
