cases:
- note: 'strings/repeat_test'
  modules:
  - |
    package test

    p = x {
      x := strings.repeat("hi", -1)
    }
  query: data.test.p = x
  note: 'count must be greater than zero'
  want_result:
  - x: "" 
