package test

import future.keywords.if

a.b := "c" {
	input.x
}

b.c := "d" if {
	input.d
}

c.d.e

d.e.f {
	input.x
}

e[f] := "g" {
	f := input.f
}

f[g] := "h" if {
	g := input.g
}

g.h[i].j[k] := "l" {
	i := input.i
	k := input.k
}

h.i[j].k[l] := "m" if {
	j := input.j
	l := input.l
}
