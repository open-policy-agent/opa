package test

import future.keywords.and
import future.keywords.or

# when `and`/`or` are keywords, the author's bracket notation is preserved
quoted_refs if {
	input["and"] == 1
	input["or"] == 2
	input.a["and"].b or input.c
}

unquoted_refs if {
	input["and"] == 1
	input["or"] == 2
}
