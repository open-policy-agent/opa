---
# Generated externalized tests from TestParseLogical_VoidCallOperandIsRejected
cases:
  - note: exprs/parse-logical-void-call-operand-is-rejected/lhs of and (module)
    module: |
      package test

      import future.keywords.and

      p if {
      	print("x") and z
      }
    want_errors:
      - code: rego_parse_error
        row: 6
        col: 2
        message: 'operand of `and` cannot consist only of calls to `print` (hint: `print` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/lhs of and (body)
    body: |
      print("x") and z
    imports:
      - future.keywords.and
    want_errors:
      - code: rego_parse_error
        row: 1
        col: 1
        message: 'operand of `and` cannot consist only of calls to `print` (hint: `print` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/rhs of and (module)
    module: |
      package test

      import future.keywords.and

      p if {
      	z and print("x")
      }
    want_errors:
      - code: rego_parse_error
        row: 6
        col: 8
        message: 'operand of `and` cannot consist only of calls to `print` (hint: `print` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/rhs of and (body)
    body: |
      z and print("x")
    imports:
      - future.keywords.and
    want_errors:
      - code: rego_parse_error
        row: 1
        col: 7
        message: 'operand of `and` cannot consist only of calls to `print` (hint: `print` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/lhs of or (module)
    module: |
      package test

      import future.keywords.or

      p if {
      	print("x") or z
      }
    want_errors:
      - code: rego_parse_error
        row: 6
        col: 2
        message: 'operand of `or` cannot consist only of calls to `print` (hint: `print` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/lhs of or (body)
    body: |
      print("x") or z
    imports:
      - future.keywords.or
    want_errors:
      - code: rego_parse_error
        row: 1
        col: 1
        message: 'operand of `or` cannot consist only of calls to `print` (hint: `print` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/rhs of or (module)
    module: |
      package test

      import future.keywords.or

      p if {
      	z or print("x")
      }
    want_errors:
      - code: rego_parse_error
        row: 6
        col: 7
        message: 'operand of `or` cannot consist only of calls to `print` (hint: `print` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/rhs of or (body)
    body: |
      z or print("x")
    imports:
      - future.keywords.or
    want_errors:
      - code: rego_parse_error
        row: 1
        col: 6
        message: 'operand of `or` cannot consist only of calls to `print` (hint: `print` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/both operands of and (module)
    module: |
      package test

      import future.keywords.and

      p if {
      	print("x") and print("x")
      }
    want_errors:
      - code: rego_parse_error
        row: 6
        col: 2
        message: 'operand of `and` cannot consist only of calls to `print` (hint: `print` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/both operands of and (body)
    body: |
      print("x") and print("x")
    imports:
      - future.keywords.and
    want_errors:
      - code: rego_parse_error
        row: 1
        col: 1
        message: 'operand of `and` cannot consist only of calls to `print` (hint: `print` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/both operands of or (module)
    module: |
      package test

      import future.keywords.or

      p if {
      	print("x") or print("x")
      }
    want_errors:
      - code: rego_parse_error
        row: 6
        col: 2
        message: 'operand of `or` cannot consist only of calls to `print` (hint: `print` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/both operands of or (body)
    body: |
      print("x") or print("x")
    imports:
      - future.keywords.or
    want_errors:
      - code: rego_parse_error
        row: 1
        col: 1
        message: 'operand of `or` cannot consist only of calls to `print` (hint: `print` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/middle of an and chain (module)
    module: |
      package test

      import future.keywords.and

      p if {
      	y and print("x") and z
      }
    want_errors:
      - code: rego_parse_error
        row: 6
        col: 8
        message: 'operand of `and` cannot consist only of calls to `print` (hint: `print` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/middle of an and chain (body)
    body: |
      y and print("x") and z
    imports:
      - future.keywords.and
    want_errors:
      - code: rego_parse_error
        row: 1
        col: 7
        message: 'operand of `and` cannot consist only of calls to `print` (hint: `print` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/middle of an or chain (module)
    module: |
      package test

      import future.keywords.or

      p if {
      	y or print("x") or z
      }
    want_errors:
      - code: rego_parse_error
        row: 6
        col: 7
        message: 'operand of `or` cannot consist only of calls to `print` (hint: `print` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/middle of an or chain (body)
    body: |
      y or print("x") or z
    imports:
      - future.keywords.or
    want_errors:
      - code: rego_parse_error
        row: 1
        col: 6
        message: 'operand of `or` cannot consist only of calls to `print` (hint: `print` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/and operand inside an or chain (module)
    module: |
      package test

      import future.keywords.and
      import future.keywords.or

      p if {
      	y or z and print("x")
      }
    want_errors:
      - code: rego_parse_error
        row: 7
        col: 13
        message: 'operand of `and` cannot consist only of calls to `print` (hint: `print` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/and operand inside an or chain (body)
    body: |
      y or z and print("x")
    imports:
      - future.keywords.and
      - future.keywords.or
    want_errors:
      - code: rego_parse_error
        row: 1
        col: 12
        message: 'operand of `and` cannot consist only of calls to `print` (hint: `print` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/parenthesized lhs of and (module)
    module: |
      package test

      import future.keywords.and

      p if {
      	(print("x")) and z
      }
    want_errors:
      - code: rego_parse_error
        row: 6
        col: 2
        message: 'operand of `and` cannot consist only of calls to `print` (hint: `print` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/parenthesized lhs of and (body)
    body: |
      (print("x")) and z
    imports:
      - future.keywords.and
    want_errors:
      - code: rego_parse_error
        row: 1
        col: 1
        message: 'operand of `and` cannot consist only of calls to `print` (hint: `print` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/explicit body operand, lhs of and (module)
    module: |
      package test

      import future.keywords.and

      p if {
      	{print("x")} and z
      }
    want_errors:
      - code: rego_parse_error
        row: 6
        col: 3
        message: 'operand of `and` cannot consist only of calls to `print` (hint: `print` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/explicit body operand, lhs of and (body)
    body: |
      {print("x")} and z
    imports:
      - future.keywords.and
    want_errors:
      - code: rego_parse_error
        row: 1
        col: 2
        message: 'operand of `and` cannot consist only of calls to `print` (hint: `print` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/explicit body operand, rhs of or (module)
    module: |
      package test

      import future.keywords.or

      p if {
      	z or {print("x")}
      }
    want_errors:
      - code: rego_parse_error
        row: 6
        col: 8
        message: 'operand of `or` cannot consist only of calls to `print` (hint: `print` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/explicit body operand, rhs of or (body)
    body: |
      z or {print("x")}
    imports:
      - future.keywords.or
    want_errors:
      - code: rego_parse_error
        row: 1
        col: 7
        message: 'operand of `or` cannot consist only of calls to `print` (hint: `print` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/explicit body of only void calls (module)
    module: |
      package test

      import future.keywords.and

      p if {
      	{print("x"); print("x")} and z
      }
    want_errors:
      - code: rego_parse_error
        row: 6
        col: 3
        message: 'operand of `and` cannot consist only of calls to `print` (hint: `print` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/explicit body of only void calls (body)
    body: |
      {print("x"); print("x")} and z
    imports:
      - future.keywords.and
    want_errors:
      - code: rego_parse_error
        row: 1
        col: 2
        message: 'operand of `and` cannot consist only of calls to `print` (hint: `print` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/lhs of and (2) (module)
    module: |
      package test

      import future.keywords.and

      p if {
      	internal.print([{"x"}]) and z
      }
    want_errors:
      - code: rego_parse_error
        row: 6
        col: 2
        message: 'operand of `and` cannot consist only of calls to `internal.print` (hint: `internal.print` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/lhs of and (2) (body)
    body: |
      internal.print([{"x"}]) and z
    imports:
      - future.keywords.and
    want_errors:
      - code: rego_parse_error
        row: 1
        col: 1
        message: 'operand of `and` cannot consist only of calls to `internal.print` (hint: `internal.print` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/rhs of and (2) (module)
    module: |
      package test

      import future.keywords.and

      p if {
      	z and internal.print([{"x"}])
      }
    want_errors:
      - code: rego_parse_error
        row: 6
        col: 8
        message: 'operand of `and` cannot consist only of calls to `internal.print` (hint: `internal.print` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/rhs of and (2) (body)
    body: |
      z and internal.print([{"x"}])
    imports:
      - future.keywords.and
    want_errors:
      - code: rego_parse_error
        row: 1
        col: 7
        message: 'operand of `and` cannot consist only of calls to `internal.print` (hint: `internal.print` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/lhs of or (2) (module)
    module: |
      package test

      import future.keywords.or

      p if {
      	internal.print([{"x"}]) or z
      }
    want_errors:
      - code: rego_parse_error
        row: 6
        col: 2
        message: 'operand of `or` cannot consist only of calls to `internal.print` (hint: `internal.print` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/lhs of or (2) (body)
    body: |
      internal.print([{"x"}]) or z
    imports:
      - future.keywords.or
    want_errors:
      - code: rego_parse_error
        row: 1
        col: 1
        message: 'operand of `or` cannot consist only of calls to `internal.print` (hint: `internal.print` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/rhs of or (2) (module)
    module: |
      package test

      import future.keywords.or

      p if {
      	z or internal.print([{"x"}])
      }
    want_errors:
      - code: rego_parse_error
        row: 6
        col: 7
        message: 'operand of `or` cannot consist only of calls to `internal.print` (hint: `internal.print` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/rhs of or (2) (body)
    body: |
      z or internal.print([{"x"}])
    imports:
      - future.keywords.or
    want_errors:
      - code: rego_parse_error
        row: 1
        col: 6
        message: 'operand of `or` cannot consist only of calls to `internal.print` (hint: `internal.print` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/both operands of and (2) (module)
    module: |
      package test

      import future.keywords.and

      p if {
      	internal.print([{"x"}]) and internal.print([{"x"}])
      }
    want_errors:
      - code: rego_parse_error
        row: 6
        col: 2
        message: 'operand of `and` cannot consist only of calls to `internal.print` (hint: `internal.print` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/both operands of and (2) (body)
    body: |
      internal.print([{"x"}]) and internal.print([{"x"}])
    imports:
      - future.keywords.and
    want_errors:
      - code: rego_parse_error
        row: 1
        col: 1
        message: 'operand of `and` cannot consist only of calls to `internal.print` (hint: `internal.print` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/both operands of or (2) (module)
    module: |
      package test

      import future.keywords.or

      p if {
      	internal.print([{"x"}]) or internal.print([{"x"}])
      }
    want_errors:
      - code: rego_parse_error
        row: 6
        col: 2
        message: 'operand of `or` cannot consist only of calls to `internal.print` (hint: `internal.print` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/both operands of or (2) (body)
    body: |
      internal.print([{"x"}]) or internal.print([{"x"}])
    imports:
      - future.keywords.or
    want_errors:
      - code: rego_parse_error
        row: 1
        col: 1
        message: 'operand of `or` cannot consist only of calls to `internal.print` (hint: `internal.print` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/middle of an and chain (2) (module)
    module: |
      package test

      import future.keywords.and

      p if {
      	y and internal.print([{"x"}]) and z
      }
    want_errors:
      - code: rego_parse_error
        row: 6
        col: 8
        message: 'operand of `and` cannot consist only of calls to `internal.print` (hint: `internal.print` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/middle of an and chain (2) (body)
    body: |
      y and internal.print([{"x"}]) and z
    imports:
      - future.keywords.and
    want_errors:
      - code: rego_parse_error
        row: 1
        col: 7
        message: 'operand of `and` cannot consist only of calls to `internal.print` (hint: `internal.print` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/middle of an or chain (2) (module)
    module: |
      package test

      import future.keywords.or

      p if {
      	y or internal.print([{"x"}]) or z
      }
    want_errors:
      - code: rego_parse_error
        row: 6
        col: 7
        message: 'operand of `or` cannot consist only of calls to `internal.print` (hint: `internal.print` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/middle of an or chain (2) (body)
    body: |
      y or internal.print([{"x"}]) or z
    imports:
      - future.keywords.or
    want_errors:
      - code: rego_parse_error
        row: 1
        col: 6
        message: 'operand of `or` cannot consist only of calls to `internal.print` (hint: `internal.print` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/and operand inside an or chain (2) (module)
    module: |
      package test

      import future.keywords.and
      import future.keywords.or

      p if {
      	y or z and internal.print([{"x"}])
      }
    want_errors:
      - code: rego_parse_error
        row: 7
        col: 13
        message: 'operand of `and` cannot consist only of calls to `internal.print` (hint: `internal.print` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/and operand inside an or chain (2) (body)
    body: |
      y or z and internal.print([{"x"}])
    imports:
      - future.keywords.and
      - future.keywords.or
    want_errors:
      - code: rego_parse_error
        row: 1
        col: 12
        message: 'operand of `and` cannot consist only of calls to `internal.print` (hint: `internal.print` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/parenthesized lhs of and (2) (module)
    module: |
      package test

      import future.keywords.and

      p if {
      	(internal.print([{"x"}])) and z
      }
    want_errors:
      - code: rego_parse_error
        row: 6
        col: 2
        message: 'operand of `and` cannot consist only of calls to `internal.print` (hint: `internal.print` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/parenthesized lhs of and (2) (body)
    body: |
      (internal.print([{"x"}])) and z
    imports:
      - future.keywords.and
    want_errors:
      - code: rego_parse_error
        row: 1
        col: 1
        message: 'operand of `and` cannot consist only of calls to `internal.print` (hint: `internal.print` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/explicit body operand, lhs of and (2) (module)
    module: |
      package test

      import future.keywords.and

      p if {
      	{internal.print([{"x"}])} and z
      }
    want_errors:
      - code: rego_parse_error
        row: 6
        col: 3
        message: 'operand of `and` cannot consist only of calls to `internal.print` (hint: `internal.print` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/explicit body operand, lhs of and (2) (body)
    body: |
      {internal.print([{"x"}])} and z
    imports:
      - future.keywords.and
    want_errors:
      - code: rego_parse_error
        row: 1
        col: 2
        message: 'operand of `and` cannot consist only of calls to `internal.print` (hint: `internal.print` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/explicit body operand, rhs of or (2) (module)
    module: |
      package test

      import future.keywords.or

      p if {
      	z or {internal.print([{"x"}])}
      }
    want_errors:
      - code: rego_parse_error
        row: 6
        col: 8
        message: 'operand of `or` cannot consist only of calls to `internal.print` (hint: `internal.print` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/explicit body operand, rhs of or (2) (body)
    body: |
      z or {internal.print([{"x"}])}
    imports:
      - future.keywords.or
    want_errors:
      - code: rego_parse_error
        row: 1
        col: 7
        message: 'operand of `or` cannot consist only of calls to `internal.print` (hint: `internal.print` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/explicit body of only void calls (2) (module)
    module: |
      package test

      import future.keywords.and

      p if {
      	{internal.print([{"x"}]); internal.print([{"x"}])} and z
      }
    want_errors:
      - code: rego_parse_error
        row: 6
        col: 3
        message: 'operand of `and` cannot consist only of calls to `internal.print` (hint: `internal.print` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/explicit body of only void calls (2) (body)
    body: |
      {internal.print([{"x"}]); internal.print([{"x"}])} and z
    imports:
      - future.keywords.and
    want_errors:
      - code: rego_parse_error
        row: 1
        col: 2
        message: 'operand of `and` cannot consist only of calls to `internal.print` (hint: `internal.print` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/lhs of and (3) (module)
    module: |
      package test

      import future.keywords.and

      p if {
      	internal.test_case(["x"]) and z
      }
    want_errors:
      - code: rego_parse_error
        row: 6
        col: 2
        message: 'operand of `and` cannot consist only of calls to `internal.test_case` (hint: `internal.test_case` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/lhs of and (3) (body)
    body: |
      internal.test_case(["x"]) and z
    imports:
      - future.keywords.and
    want_errors:
      - code: rego_parse_error
        row: 1
        col: 1
        message: 'operand of `and` cannot consist only of calls to `internal.test_case` (hint: `internal.test_case` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/rhs of and (3) (module)
    module: |
      package test

      import future.keywords.and

      p if {
      	z and internal.test_case(["x"])
      }
    want_errors:
      - code: rego_parse_error
        row: 6
        col: 8
        message: 'operand of `and` cannot consist only of calls to `internal.test_case` (hint: `internal.test_case` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/rhs of and (3) (body)
    body: |
      z and internal.test_case(["x"])
    imports:
      - future.keywords.and
    want_errors:
      - code: rego_parse_error
        row: 1
        col: 7
        message: 'operand of `and` cannot consist only of calls to `internal.test_case` (hint: `internal.test_case` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/lhs of or (3) (module)
    module: |
      package test

      import future.keywords.or

      p if {
      	internal.test_case(["x"]) or z
      }
    want_errors:
      - code: rego_parse_error
        row: 6
        col: 2
        message: 'operand of `or` cannot consist only of calls to `internal.test_case` (hint: `internal.test_case` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/lhs of or (3) (body)
    body: |
      internal.test_case(["x"]) or z
    imports:
      - future.keywords.or
    want_errors:
      - code: rego_parse_error
        row: 1
        col: 1
        message: 'operand of `or` cannot consist only of calls to `internal.test_case` (hint: `internal.test_case` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/rhs of or (3) (module)
    module: |
      package test

      import future.keywords.or

      p if {
      	z or internal.test_case(["x"])
      }
    want_errors:
      - code: rego_parse_error
        row: 6
        col: 7
        message: 'operand of `or` cannot consist only of calls to `internal.test_case` (hint: `internal.test_case` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/rhs of or (3) (body)
    body: |
      z or internal.test_case(["x"])
    imports:
      - future.keywords.or
    want_errors:
      - code: rego_parse_error
        row: 1
        col: 6
        message: 'operand of `or` cannot consist only of calls to `internal.test_case` (hint: `internal.test_case` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/both operands of and (3) (module)
    module: |
      package test

      import future.keywords.and

      p if {
      	internal.test_case(["x"]) and internal.test_case(["x"])
      }
    want_errors:
      - code: rego_parse_error
        row: 6
        col: 2
        message: 'operand of `and` cannot consist only of calls to `internal.test_case` (hint: `internal.test_case` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/both operands of and (3) (body)
    body: |
      internal.test_case(["x"]) and internal.test_case(["x"])
    imports:
      - future.keywords.and
    want_errors:
      - code: rego_parse_error
        row: 1
        col: 1
        message: 'operand of `and` cannot consist only of calls to `internal.test_case` (hint: `internal.test_case` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/both operands of or (3) (module)
    module: |
      package test

      import future.keywords.or

      p if {
      	internal.test_case(["x"]) or internal.test_case(["x"])
      }
    want_errors:
      - code: rego_parse_error
        row: 6
        col: 2
        message: 'operand of `or` cannot consist only of calls to `internal.test_case` (hint: `internal.test_case` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/both operands of or (3) (body)
    body: |
      internal.test_case(["x"]) or internal.test_case(["x"])
    imports:
      - future.keywords.or
    want_errors:
      - code: rego_parse_error
        row: 1
        col: 1
        message: 'operand of `or` cannot consist only of calls to `internal.test_case` (hint: `internal.test_case` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/middle of an and chain (3) (module)
    module: |
      package test

      import future.keywords.and

      p if {
      	y and internal.test_case(["x"]) and z
      }
    want_errors:
      - code: rego_parse_error
        row: 6
        col: 8
        message: 'operand of `and` cannot consist only of calls to `internal.test_case` (hint: `internal.test_case` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/middle of an and chain (3) (body)
    body: |
      y and internal.test_case(["x"]) and z
    imports:
      - future.keywords.and
    want_errors:
      - code: rego_parse_error
        row: 1
        col: 7
        message: 'operand of `and` cannot consist only of calls to `internal.test_case` (hint: `internal.test_case` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/middle of an or chain (3) (module)
    module: |
      package test

      import future.keywords.or

      p if {
      	y or internal.test_case(["x"]) or z
      }
    want_errors:
      - code: rego_parse_error
        row: 6
        col: 7
        message: 'operand of `or` cannot consist only of calls to `internal.test_case` (hint: `internal.test_case` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/middle of an or chain (3) (body)
    body: |
      y or internal.test_case(["x"]) or z
    imports:
      - future.keywords.or
    want_errors:
      - code: rego_parse_error
        row: 1
        col: 6
        message: 'operand of `or` cannot consist only of calls to `internal.test_case` (hint: `internal.test_case` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/and operand inside an or chain (3) (module)
    module: |
      package test

      import future.keywords.and
      import future.keywords.or

      p if {
      	y or z and internal.test_case(["x"])
      }
    want_errors:
      - code: rego_parse_error
        row: 7
        col: 13
        message: 'operand of `and` cannot consist only of calls to `internal.test_case` (hint: `internal.test_case` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/and operand inside an or chain (3) (body)
    body: |
      y or z and internal.test_case(["x"])
    imports:
      - future.keywords.and
      - future.keywords.or
    want_errors:
      - code: rego_parse_error
        row: 1
        col: 12
        message: 'operand of `and` cannot consist only of calls to `internal.test_case` (hint: `internal.test_case` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/parenthesized lhs of and (3) (module)
    module: |
      package test

      import future.keywords.and

      p if {
      	(internal.test_case(["x"])) and z
      }
    want_errors:
      - code: rego_parse_error
        row: 6
        col: 2
        message: 'operand of `and` cannot consist only of calls to `internal.test_case` (hint: `internal.test_case` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/parenthesized lhs of and (3) (body)
    body: |
      (internal.test_case(["x"])) and z
    imports:
      - future.keywords.and
    want_errors:
      - code: rego_parse_error
        row: 1
        col: 1
        message: 'operand of `and` cannot consist only of calls to `internal.test_case` (hint: `internal.test_case` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/explicit body operand, lhs of and (3) (module)
    module: |
      package test

      import future.keywords.and

      p if {
      	{internal.test_case(["x"])} and z
      }
    want_errors:
      - code: rego_parse_error
        row: 6
        col: 3
        message: 'operand of `and` cannot consist only of calls to `internal.test_case` (hint: `internal.test_case` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/explicit body operand, lhs of and (3) (body)
    body: |
      {internal.test_case(["x"])} and z
    imports:
      - future.keywords.and
    want_errors:
      - code: rego_parse_error
        row: 1
        col: 2
        message: 'operand of `and` cannot consist only of calls to `internal.test_case` (hint: `internal.test_case` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/explicit body operand, rhs of or (3) (module)
    module: |
      package test

      import future.keywords.or

      p if {
      	z or {internal.test_case(["x"])}
      }
    want_errors:
      - code: rego_parse_error
        row: 6
        col: 8
        message: 'operand of `or` cannot consist only of calls to `internal.test_case` (hint: `internal.test_case` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/explicit body operand, rhs of or (3) (body)
    body: |
      z or {internal.test_case(["x"])}
    imports:
      - future.keywords.or
    want_errors:
      - code: rego_parse_error
        row: 1
        col: 7
        message: 'operand of `or` cannot consist only of calls to `internal.test_case` (hint: `internal.test_case` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/explicit body of only void calls (3) (module)
    module: |
      package test

      import future.keywords.and

      p if {
      	{internal.test_case(["x"]); internal.test_case(["x"])} and z
      }
    want_errors:
      - code: rego_parse_error
        row: 6
        col: 3
        message: 'operand of `and` cannot consist only of calls to `internal.test_case` (hint: `internal.test_case` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/explicit body of only void calls (3) (body)
    body: |
      {internal.test_case(["x"]); internal.test_case(["x"])} and z
    imports:
      - future.keywords.and
    want_errors:
      - code: rego_parse_error
        row: 1
        col: 2
        message: 'operand of `and` cannot consist only of calls to `internal.test_case` (hint: `internal.test_case` produces no value and always succeeds, so the operand can never fail; move it out of the operand, or add an expression that can fail)'
  - note: exprs/parse-logical-void-call-operand-is-rejected/{print("x"); true} and z
    body: |
      {print("x"); true} and z
    imports:
      - future.keywords.and
      - future.keywords.or
      - future.keywords.not
    want_ast: |
      [
        {
          "index": 0,
          "terms": {
            "explicit_lhs": true,
            "lhs": [
              {
                "index": 0,
                "terms": [
                  {
                    "type": "ref",
                    "value": [
                      {
                        "type": "var",
                        "value": "print"
                      }
                    ]
                  },
                  {
                    "type": "string",
                    "value": "x"
                  }
                ]
              },
              {
                "index": 1,
                "terms": {
                  "type": "boolean",
                  "value": true
                }
              }
            ],
            "rhs": [
              {
                "index": 0,
                "terms": {
                  "type": "var",
                  "value": "z"
                }
              }
            ],
            "type": "and"
          }
        }
      ]
  - note: exprs/parse-logical-void-call-operand-is-rejected/{some v in [1]; print(v)} and z
    body: |
      {some v in [1]; print(v)} and z
    imports:
      - future.keywords.and
      - future.keywords.or
      - future.keywords.not
    want_ast: |
      [
        {
          "index": 0,
          "terms": {
            "explicit_lhs": true,
            "lhs": [
              {
                "index": 0,
                "terms": {
                  "symbols": [
                    {
                      "type": "call",
                      "value": [
                        {
                          "type": "ref",
                          "value": [
                            {
                              "type": "var",
                              "value": "internal"
                            },
                            {
                              "type": "string",
                              "value": "member_2"
                            }
                          ]
                        },
                        {
                          "type": "var",
                          "value": "v"
                        },
                        {
                          "type": "array",
                          "value": [
                            {
                              "type": "number",
                              "value": 1
                            }
                          ]
                        }
                      ]
                    }
                  ]
                }
              },
              {
                "index": 1,
                "terms": [
                  {
                    "type": "ref",
                    "value": [
                      {
                        "type": "var",
                        "value": "print"
                      }
                    ]
                  },
                  {
                    "type": "var",
                    "value": "v"
                  }
                ]
              }
            ],
            "rhs": [
              {
                "index": 0,
                "terms": {
                  "type": "var",
                  "value": "z"
                }
              }
            ],
            "type": "and"
          }
        }
      ]
  - note: exprs/parse-logical-void-call-operand-is-rejected/not print("x") and z
    body: |
      not print("x") and z
    imports:
      - future.keywords.and
      - future.keywords.or
      - future.keywords.not
    want_ast: |
      [
        {
          "index": 0,
          "terms": {
            "lhs": [
              {
                "index": 0,
                "terms": {
                  "body": [
                    {
                      "index": 0,
                      "terms": [
                        {
                          "type": "ref",
                          "value": [
                            {
                              "type": "var",
                              "value": "print"
                            }
                          ]
                        },
                        {
                          "type": "string",
                          "value": "x"
                        }
                      ]
                    }
                  ],
                  "explicit_body": false,
                  "type": "not"
                }
              }
            ],
            "rhs": [
              {
                "index": 0,
                "terms": {
                  "type": "var",
                  "value": "z"
                }
              }
            ],
            "type": "and"
          }
        }
      ]
  - note: exprs/parse-logical-void-call-operand-is-rejected/not {print("x")} and z
    body: |
      not {print("x")} and z
    imports:
      - future.keywords.and
      - future.keywords.or
      - future.keywords.not
    want_ast: |
      [
        {
          "index": 0,
          "terms": {
            "lhs": [
              {
                "index": 0,
                "terms": {
                  "body": [
                    {
                      "index": 0,
                      "terms": [
                        {
                          "type": "ref",
                          "value": [
                            {
                              "type": "var",
                              "value": "print"
                            }
                          ]
                        },
                        {
                          "type": "string",
                          "value": "x"
                        }
                      ]
                    }
                  ],
                  "explicit_body": true,
                  "type": "not"
                }
              }
            ],
            "rhs": [
              {
                "index": 0,
                "terms": {
                  "type": "var",
                  "value": "z"
                }
              }
            ],
            "type": "and"
          }
        }
      ]
  - note: exprs/parse-logical-void-call-operand-is-rejected/x := [1 | print("x")] and z
    body: |
      x := [1 | print("x")] and z
    imports:
      - future.keywords.and
      - future.keywords.or
      - future.keywords.not
    want_ast: |
      [
        {
          "index": 0,
          "terms": {
            "lhs": [
              {
                "index": 0,
                "terms": [
                  {
                    "type": "ref",
                    "value": [
                      {
                        "type": "var",
                        "value": "assign"
                      }
                    ]
                  },
                  {
                    "type": "var",
                    "value": "x"
                  },
                  {
                    "type": "arraycomprehension",
                    "value": {
                      "body": [
                        {
                          "index": 0,
                          "terms": [
                            {
                              "type": "ref",
                              "value": [
                                {
                                  "type": "var",
                                  "value": "print"
                                }
                              ]
                            },
                            {
                              "type": "string",
                              "value": "x"
                            }
                          ]
                        }
                      ],
                      "term": {
                        "type": "number",
                        "value": 1
                      }
                    }
                  }
                ]
              }
            ],
            "rhs": [
              {
                "index": 0,
                "terms": {
                  "type": "var",
                  "value": "z"
                }
              }
            ],
            "type": "and"
          }
        }
      ]
  - note: exprs/parse-logical-void-call-operand-is-rejected/print(f(x)) ; z
    body: |
      print(f(x)) ; z
    imports:
      - future.keywords.and
      - future.keywords.or
      - future.keywords.not
    want_ast: |
      [
        {
          "index": 0,
          "terms": [
            {
              "type": "ref",
              "value": [
                {
                  "type": "var",
                  "value": "print"
                }
              ]
            },
            {
              "type": "call",
              "value": [
                {
                  "type": "ref",
                  "value": [
                    {
                      "type": "var",
                      "value": "f"
                    }
                  ]
                },
                {
                  "type": "var",
                  "value": "x"
                }
              ]
            }
          ]
        },
        {
          "index": 1,
          "terms": {
            "type": "var",
            "value": "z"
          }
        }
      ]
