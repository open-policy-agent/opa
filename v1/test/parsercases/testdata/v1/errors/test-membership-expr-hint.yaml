---
# Generated externalized tests from TestMembershipExprHint
cases:
  - note: errors/membership-expr-hint/unrelated comma error gets no hint
    module: |
      package test

      p {
      	x, y
      }
    want_errors:
      - code: rego_parse_error
        row: 4
        col: 3
        message: 'unexpected , token: expected \n or ; or }'
