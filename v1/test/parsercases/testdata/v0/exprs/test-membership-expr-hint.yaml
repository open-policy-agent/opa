---
# Generated externalized tests from TestMembershipExprHint
cases:
  - note: exprs/membership-expr-hint/bare membership expr (module)
    module: |
      package test

      p {
      	input.request.operation in {"CREATE", "UPDATE"}
      }
    want_errors:
      - code: rego_parse_error
        row: 4
        col: 26
        message: 'unexpected identifier token: expected \n or ; or } (hint: `import future.keywords.in` for `x in xs` expressions)'
  - note: exprs/membership-expr-hint/negated (module)
    module: |
      package test

      p {
      	not x in xs
      }
    want_errors:
      - code: rego_parse_error
        row: 4
        col: 8
        message: 'unexpected identifier token: expected \n or ; or } (hint: `import future.keywords.in` for `x in xs` expressions)'
  - note: exprs/membership-expr-hint/semicolon separated (module)
    module: |
      package test

      p {
      	x in xs; x
      }
    want_errors:
      - code: rego_parse_error
        row: 4
        col: 4
        message: 'unexpected identifier token: expected \n or ; or } (hint: `import future.keywords.in` for `x in xs` expressions)'
  - note: exprs/membership-expr-hint/comprehension body (module)
    module: |
      package test

      p {
      	ys := [y | y in xs]
      }
    want_errors:
      - code: rego_parse_error
        row: 4
        col: 15
        message: 'unexpected identifier token: expected \n or ; or ] (hint: `import future.keywords.in` for `x in xs` expressions)'
  - note: exprs/membership-expr-hint/assigned (module)
    module: |
      package test

      p {
      	x := y in ys
      }
    want_errors:
      - code: rego_parse_error
        row: 4
        col: 9
        message: 'unexpected identifier token: expected \n or ; or } (hint: `import future.keywords.in` for `x in xs` expressions)'
  - note: exprs/membership-expr-hint/unified (module)
    module: |
      package test

      p {
      	x = y in ys
      }
    want_errors:
      - code: rego_parse_error
        row: 4
        col: 8
        message: 'unexpected identifier token: expected \n or ; or } (hint: `import future.keywords.in` for `x in xs` expressions)'
  - note: exprs/membership-expr-hint/key/value membership (module)
    module: |
      package test

      p {
      	"k", v in {"k": "v"}
      }
    want_errors:
      - code: rego_parse_error
        row: 4
        col: 5
        message: 'unexpected , token: expected \n or ; or } (hint: `import future.keywords.in` for `x in xs` expressions)'
