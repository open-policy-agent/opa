---
# Generated externalized tests from TestCompilerCheckDeprecatedMethods
cases:
  - note: builtins/all-built-in-non-strict
    rego_version: v0
    strict: disabled
    modules:
      - |
        package test

        p := all([true, false])
    want:
      - module: |
          package test

          p := __local0__ {
          	all([true, false], __local0__)
          }
  - note: builtins/any-built-in-non-strict
    rego_version: v0
    strict: disabled
    modules:
      - |
        package test

        p := any([true, false])
    want:
      - module: |
          package test

          p := __local0__ {
          	any([true, false], __local0__)
          }
  - note: builtins/re-match-built-in-non-strict
    rego_version: v0
    strict: disabled
    modules:
      - |-
        package test

        p := re_match("[a]", "a")
    want:
      - module: |
          package test

          p := __local0__ {
          	re_match("[a]", "a", __local0__)
          }
