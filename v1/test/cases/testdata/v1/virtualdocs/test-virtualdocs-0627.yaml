---
cases:
  - note: "virtualdocs/input: set ground composite (2)"
    query: data.generated.p = x
    modules:
      - |
        package generated

        p if {
        	y = 2
        	z = [[1, y], y]
        	data.generated.q[z]
        }

        q contains [x, y] if {
        	y = 2
        	x = [1, y]
        }
    data: {}
    want_result:
      - x: true
