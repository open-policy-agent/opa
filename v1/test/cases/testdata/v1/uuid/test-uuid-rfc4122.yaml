---
cases:
  - note: uuid.rfc4122/consistent values for same arguments
    query: data.test.p = x
    modules:
      - |
        package test

        p := count(uuids) if {
        	uuids := {uuid.rfc4122("key") | numbers.range(1, 100)[_]}
        }
    want_result:
      - x: 1
