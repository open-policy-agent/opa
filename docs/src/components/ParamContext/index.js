import React from "react";

// ParamContext is a context created to manage editable params on a
// documentation pages, this is used to manage the state of the editable params
// in other components.
const ParamContext = React.createContext();

export default ParamContext;
