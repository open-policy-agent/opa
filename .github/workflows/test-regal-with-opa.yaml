name: Test Regal with OPA

on:
  workflow_call:
    secrets:
      slack_webhook_url:
        required: false

permissions:
  contents: read

jobs:
  test-regal-with-opa:
    name: Test Regal with OPA
    runs-on: ubuntu-24.04
    env:
      SLACK_WEBHOOK_URL: ${{ secrets.slack_webhook_url }} # zizmor: ignore[secrets-outside-env]
    steps:
      - name: Check out OPA code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
          path: opa

      - name: Install Go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: opa/.go-version
          cache-dependency-path: opa/go.sum

      - name: Check out Regal main
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          repository: open-policy-agent/regal
          ref: main
          persist-credentials: false
          path: regal

      - name: Update OPA dependency
        working-directory: regal
        run: |
          go mod edit -replace github.com/open-policy-agent/opa=../opa
          go mod tidy

      - name: Build
        working-directory: regal
        run: go build -tags=regal_standalone

      # Regal's build tasks are written in Rego and run by rq, which `go tool`
      # builds from Regal's module graph, i.e. against the OPA under test.
      # Running the tests through build/do.rq makes the nightly parse and
      # evaluate that script too, which is where the bug fixed in #9140
      # surfaced.
      - name: Test
        working-directory: regal
        run: build/do.rq test

      - name: e2e tests
        working-directory: regal
        run: build/do.rq e2e

      - name: Slack Notification
        uses: slackapi/slack-github-action@dcb1066f776dd043e64d0e8ba94ca15cc7e1875d # v4.0.0
        if: failure() && env.SLACK_WEBHOOK_URL != ''
        with:
          webhook: ${{ env.SLACK_WEBHOOK_URL }} # zizmor: ignore[secrets-outside-env]
          webhook-type: incoming-webhook
          payload: |
            {
              "text": "Regal compatibility check failed: <${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}|${{ github.workflow }} - ${{ github.job }}>"
            }
